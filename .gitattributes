# Auto detect text files and perform LF normalization
* text=auto

# Force LF for shell scripts and config files used inside the container
*.sh        text eol=lf
*.json      text eol=lf
Dockerfile  text eol=lf
